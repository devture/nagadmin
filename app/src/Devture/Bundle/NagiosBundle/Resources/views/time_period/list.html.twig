{% if items|length == 0 %}
	No items found.
{% else %}
	<table class="table table-bordered table-striped table-condensed">
		<thead>
			<tr>
				<th>Title</th>
				<th class="d-none d-sm-table-cell">Time Period</th>
			</tr>
		</thead>
		<tbody>
		{% for entity in items %}
			{% set link = path('devture_nagios.time_period.edit', {'id': entity.id}) %}
			<tr>
				<td>
					<a href="{{ link }}" class="d-block">
						{{ entity.title }}
					</a>
				</td>
				<td class="d-none d-sm-table-cell">
					{% include '@DevtureNagios/time_period/rules_table.html.twig' with {'rules': entity.rules} %}
				</td>
			</tr>
		{% endfor %}
		</tbody>
	</table>
{% endif %}
