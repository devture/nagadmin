{% if items|length == 0 %}
	No items found.
{% else %}
	<table class="table table-bordered table-striped table-condensed">
		<thead>
			<tr>
				<th class="d-none d-sm-table-cell">Identifier</th>
				<th>Title</th>
			</tr>
		</thead>
		<tbody>
		{% for entity in items %}
			{% set link = path('devture_nagios.command.edit', {'id': entity.id}) %}
			<tr>
				<td class="d-none d-sm-table-cell">
					<a href="{{ link }}" class="d-block">
						{{ entity.name }}
					</a>
				</td>
				<td>
					<a href="{{ link }}" class="d-block">
						{{ entity.title }}
					</a>
				</td>
			</tr>
		{% endfor %}
		</tbody>
	</table>
{% endif %}
