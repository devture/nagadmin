memory_limit = 384M

; Never render PHP errors/warnings/deprecations into the HTTP response.
; The base image loads no php.ini, so display_errors defaults to On, which
; corrupts responses (e.g. deprecation notices printed before redirect/cookie
; headers break login). Errors still go to the container's stderr log.
display_errors = Off
log_errors = On
