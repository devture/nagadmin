memory_limit = 384M

; Never render PHP errors/warnings/deprecations into the HTTP response: they
; corrupt responses (e.g. a deprecation notice printed before redirect/cookie
; headers breaks login). Errors still go to the container's stderr log.
display_errors = Off
log_errors = On
