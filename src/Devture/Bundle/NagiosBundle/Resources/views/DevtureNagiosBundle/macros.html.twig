{% macro service_label_class_from_state(state) %}
{% spaceless %}
	{% set map = {'ok': 'label-success', 'warning': 'label-warning', 'critical': 'label-important', 'missing': ''} %}
	{{ map[state] is defined ? map[state] : map['missing'] }}
{% endspaceless %}
{% endmacro %}